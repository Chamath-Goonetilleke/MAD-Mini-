<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madminiproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADMiniProject">
        <activity android:name=".activities.MainActivity8"></activity>
        <activity android:name=".activities.MainActivity7">

        </activity>
        <activity android:name=".activities.MainActivity6" />
        <activity android:name=".activities.MainActivity5"></activity>
        <activity
            android:name=".activities.MainActivity4"
            android:theme="@style/Theme.MaterialComponents.NoActionBar">

        </activity>
        <activity android:name=".activities.MainActivity3" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".activities.MainActivity2">
            android:parentActivityName=".MainActivity">
            <!--
            The meta-data tag is required if you support API level 15
            and lower
            -->

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>